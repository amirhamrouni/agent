name: Hayat Tounes Android RC (legacy manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-workflow-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: |
          echo 'This legacy workflow is intentionally manual-only.'
          echo 'Use .github/workflows/hayat-tounes-android-xz.yml for the active reconstructed-source Godot 4.7.2 Android RC pipeline.'
